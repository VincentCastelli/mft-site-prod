import React from 'react';
// import PropTypes from 'prop-types';

const Book = () => {
  return (
    <section className="billboard">
      <h2>Book An Appointment</h2>
      <p>Find a open time block below:</p>
    </section>
  );
};

// Book.propTypes = {
//   client: PropTypes.string.isRequired,
// };

export default Book;
